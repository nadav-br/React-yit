import React from 'react'

function Input(props) {

    // let ref = React.createRef();

    // const handleInput = (e) => {
    //     e.preventDefault();
    //     props.addContent(ref.current.value)
    //     ref.current.value = '';
    // }
    // console.log(props)
    return (
        <div>
            {/* <form onSubmit={handleInput}>
                <input type="text" placeholder="הכנס תוכן" ref={ref} />
            </form>
            <form onSubmit={handleInput}>
                <input type="text" placeholder="הכנס תוכן" ref={ref} />
            </form>
            <form onSubmit={handleInput}>
                <input type="text" placeholder="הכנס תוכן" ref={ref} />
            </form> */}
        </div>
    )
}

export default Input
